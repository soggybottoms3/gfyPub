# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository status

This repository (`gfyPub` — "Public GFY repo") is currently a skeleton: it contains only `README.md`. There is no source code, build tooling, dependency manifest, or test suite yet.

As the project takes shape, update this file with:

- **Commands** — how to build, run, lint, and test (including how to run a single test) once tooling exists.
- **Architecture** — the big-picture structure and how the major pieces fit together, the kind of context that requires reading several files to reconstruct.

Until then, there are no project-specific commands or conventions to document beyond standard git usage.
